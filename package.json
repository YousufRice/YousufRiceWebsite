{
  "name": "yousuf-rice",
  "version": "0.1.0",
  "private": true,
  "description": "Yousuf Rice monorepo — Next.js web app + Expo mobile app",
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build && rm -rf .next && cp -rL apps/web/.next .next && cp apps/web/next.config.ts next.config.ts",
    "lint": "turbo run lint",
    "web": "pnpm --filter @yousuf-rice/web dev",
    "mobile": "pnpm --filter @yousuf-rice/mobile start",
    "mobile:ios": "pnpm --filter @yousuf-rice/mobile ios",
    "mobile:android": "pnpm --filter @yousuf-rice/mobile android",
    "mobile:dlx": "cd apps/mobile && pnpm dlx expo start",
    "setup:env": "node scripts/setup-env.js",
    "setup:appwrite": "node scripts/setup-appwrite-collections.js"
  },
  "devDependencies": {
    "turbo": "^2.5.2",
    "typescript": "^5.9.3"
  },
  "packageManager": "pnpm@11.0.9"
}
