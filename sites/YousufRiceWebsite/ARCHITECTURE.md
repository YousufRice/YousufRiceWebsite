# 🏗️ Yousuf Rice - Application Architecture

## System Overview

```
┌─────────────────────────────────────────────────────────────┐
│                     YOUSUF RICE PLATFORM                     │
│                  Full-Stack E-Commerce System                │
└─────────────────────────────────────────────────────────────┘

┌──────────────────┐         ┌──────────────────┐
│   CUSTOMER UI    │         │    ADMIN UI      │
│                  │         │                  │
│  • Home Page     │         │  • Dashboard     │
│  • Products      │         │  • Products CRUD │
│  • Cart          │         │  • Orders Mgmt   │
│  • Checkout      │         │  • Customers     │
│  • Orders        │         │                  │
└────────┬─────────┘         └────────┬─────────┘
         │                            │
         └────────────┬───────────────┘
                      │
         ┌────────────▼─────────────┐
         │   NEXT.JS 15 APP ROUTER  │
         │   • Server Components    │
         │   • Client Components    │
         │   • API Routes           │
         └────────────┬─────────────┘
                      │
         ┌────────────▼─────────────┐
         │   STATE MANAGEMENT       │
         │   • Zustand (Cart)       │
         │   • Zustand (Auth)       │
         └────────────┬─────────────┘
                      │
         ┌────────────▼─────────────┐
         │   APPWRITE BACKEND       │
         │   • Database (TablesDB)  │
         │   • Storage (Images)     │
         │   • Authentication       │
         └──────────────────────────┘
```

## Component Architecture

```
┌─────────────────────────────────────────────────────────────┐
│                        APP STRUCTURE                         │
└─────────────────────────────────────────────────────────────┘

app/
├── layout.tsx (Root Layout)
│   ├── <Navbar />
│   ├── <main>{children}</main>
│   └── <Toaster />
│
├── page.tsx (Home)
│   └── Hero + Features + Pricing Info
│
├── products/page.tsx (Server Component)
│   └── Fetches products from Appwrite
│       └── Maps to <ProductCard />
│
├── cart/page.tsx (Client Component)
│   └── Uses useCartStore()
│       └── Displays cart items
│           └── Checkout button
│
├── checkout/page.tsx (Client Component)
│   └── Form + GPS Location
│       └── Creates Order + Customer + Address
│
├── orders/
│   ├── page.tsx (Client Component)
│   │   └── Lists user orders
│   └── [id]/page.tsx (Server Component)
│       └── Shows order details
│
├── auth/
│   ├── login/page.tsx
│   └── register/page.tsx
│
└── admin/ (Protected by isAdmin)
    ├── page.tsx (Dashboard)
    ├── products/page.tsx (CRUD)
    ├── orders/page.tsx (Management)
    └── customers/page.tsx (List)
```

## Data Flow Architecture

### Customer Order Flow

```
┌──────────┐     ┌──────────┐     ┌──────────┐     ┌──────────┐
│ Browse   │────▶│ Add to   │────▶│ Checkout │────▶│ Place    │
│ Products │     │ Cart     │     │ Form     │     │ Order    │
└──────────┘     └──────────┘     └──────────┘     └──────────┘
     │                │                 │                 │
     ▼                ▼                 ▼                 ▼
┌──────────┐     ┌──────────┐     ┌──────────┐     ┌──────────┐
│ Appwrite │     │ Zustand  │     │ Browser  │     │ Appwrite │
│ Database │     │ Store    │     │ Geoloc   │     │ Database │
└──────────┘     └──────────┘     └──────────┘     └──────────┘
                                                          │
                                                          ▼
                                                    ┌──────────┐
                                                    │ Creates: │
                                                    │ • Order  │
                                                    │ • Customer│
                                                    │ • Address│
                                                    └──────────┘
```

### Admin Product Management Flow

```
┌──────────┐     ┌──────────┐     ┌──────────┐     ┌──────────┐
│ Admin    │────▶│ Fill     │────▶│ Upload   │────▶│ Create   │
│ Login    │     │ Form     │     │ Image    │     │ Product  │
└──────────┘     └──────────┘     └──────────┘     └──────────┘
     │                │                 │                 │
     ▼                ▼                 ▼                 ▼
┌──────────┐     ┌──────────┐     ┌──────────┐     ┌──────────┐
│ Check    │     │ Validate │     │ Appwrite │     │ Appwrite │
│ isAdmin  │     │ Input    │     │ Storage  │     │ Database │
└──────────┘     └──────────┘     └──────────┘     └──────────┘
                                        │                 │
                                        ▼                 ▼
                                   ┌──────────┐     ┌──────────┐
                                   │ Returns  │     │ Creates: │
                                   │ File ID  │     │ • Product│
                                   └──────────┘     │ • Image  │
                                                    └──────────┘
```

## Database Schema

```
┌─────────────────────────────────────────────────────────────┐
│                      APPWRITE DATABASE                       │
└─────────────────────────────────────────────────────────────┘

┌──────────────┐         ┌──────────────┐
│   PRODUCTS   │◄────────│PRODUCT_IMAGES│
│              │  1:n    │              │
│ • id         │         │ • id         │
│ • name       │         │ • product_id │
│ • description│         │ • file_id    │
│ • base_price │         │ • is_primary │
│ • tier_*     │         └──────────────┘
│ • available  │                │
└──────┬───────┘                │
       │                        │
       │ n:m                    │
       │ (via CSV)              │
       │                        │
┌──────▼───────┐         ┌──────▼───────┐
│   ORDERS     │         │   STORAGE    │
│              │         │   BUCKET     │
│ • id         │         │              │
│ • customer_id│         │ • file_id    │
│ • address_id │         │ • file_data  │
│ • order_items│         └──────────────┘
│ • total_price│
│ • status     │
└──────┬───────┘
       │
       │ n:1
       │
┌──────▼───────┐         ┌──────────────┐
│  CUSTOMERS   │────────▶│  ADDRESSES   │
│              │  1:n    │              │
│ • id         │         │ • id         │
│ • user_id    │         │ • customer_id│
│ • full_name  │         │ • order_id   │
│ • phone      │         │ • address_line│
│ • email      │         │ • latitude   │
└──────────────┘         │ • longitude  │
                         │ • maps_url   │
                         └──────────────┘
```

## State Management

```
┌─────────────────────────────────────────────────────────────┐
│                    ZUSTAND STORES                            │
└─────────────────────────────────────────────────────────────┘

┌──────────────────────┐         ┌──────────────────────┐
│    CART STORE        │         │    AUTH STORE        │
│                      │         │                      │
│ State:               │         │ State:               │
│ • items[]            │         │ • user               │
│                      │         │ • isAdmin            │
│ Actions:             │         │ • loading            │
│ • addItem()          │         │                      │
│ • removeItem()       │         │ Actions:             │
│ • updateQuantity()   │         │ • checkAuth()        │
│ • clearCart()        │         │ • logout()           │
│ • getTotalPrice()    │         │                      │
│ • getTotalItems()    │         │                      │
│                      │         │                      │
│ Persistence:         │         │ Persistence:         │
│ • localStorage       │         │ • Appwrite Session   │
└──────────────────────┘         └──────────────────────┘
```

## Authentication Flow

```
┌──────────────────────────────────────────────────────────────┐
│                    AUTHENTICATION FLOW                        │
└──────────────────────────────────────────────────────────────┘

1. User Registration
   ┌─────────┐     ┌─────────┐     ┌─────────┐
   │ Register│────▶│ Appwrite│────▶│ Create  │
   │ Form    │     │ Auth    │     │ Session │
   └─────────┘     └─────────┘     └─────────┘

2. User Login
   ┌─────────┐     ┌─────────┐     ┌─────────┐     ┌─────────┐
   │ Login   │────▶│ Appwrite│────▶│ Create  │────▶│ Check   │
   │ Form    │     │ Auth    │     │ Session │     │ Prefs   │
   └─────────┘     └─────────┘     └─────────┘     └─────────┘
                                                          │
                                                          ▼
                                                    ┌─────────┐
                                                    │ Set     │
                                                    │ isAdmin │
                                                    └─────────┘

3. Protected Route Access
   ┌─────────┐     ┌─────────┐     ┌─────────┐
   │ Access  │────▶│ Check   │────▶│ Allow/  │
   │ Route   │     │ Auth    │     │ Redirect│
   └─────────┘     └─────────┘     └─────────┘
```

## Tier Pricing Logic

```
┌──────────────────────────────────────────────────────────────┐
│                    TIER PRICING ALGORITHM                     │
└──────────────────────────────────────────────────────────────┘

Input: Product + Quantity

┌─────────────────┐
│ has_tier_pricing│
│ = true?         │
└────────┬────────┘
         │
    ┌────▼────┐
    │   YES   │
    └────┬────┘
         │
    ┌────▼────────────────────┐
    │ Quantity >= 10?         │
    │ Use tier_10kg_up_price  │
    └────┬────────────────────┘
         │ NO
    ┌────▼────────────────────┐
    │ Quantity >= 5?          │
    │ Use tier_5_9kg_price    │
    └────┬────────────────────┘
         │ NO
    ┌────▼────────────────────┐
    │ Quantity >= 2?          │
    │ Use tier_2_4kg_price    │
    └────┬────────────────────┘
         │ NO
         │
    ┌────▼────────────────────┐
    │ Use base_price_per_kg   │
    └─────────────────────────┘

Output: Price per KG × Quantity = Total Price
```

## Order Status Workflow

```
┌──────────────────────────────────────────────────────────────┐
│                    ORDER STATUS WORKFLOW                      │
└──────────────────────────────────────────────────────────────┘

┌─────────┐
│ PENDING │ ◄─── Customer places order
└────┬────┘
     │
     │ Admin clicks "Accept Order"
     │
┌────▼────┐
│ACCEPTED │
└────┬────┘
     │
     │ Admin clicks "Mark Out for Delivery"
     │
┌────▼──────────────┐
│OUT_FOR_DELIVERY   │
└────┬──────────────┘
     │
     │ Admin clicks "Mark Delivered"
     │
┌────▼─────┐
│DELIVERED │
└──────────┘

Each status change:
• Updates database
• Triggers UI refresh
• Shows in customer order tracking
```

## Security Architecture

```
┌──────────────────────────────────────────────────────────────┐
│                    SECURITY LAYERS                            │
└──────────────────────────────────────────────────────────────┘

Layer 1: Route Protection
┌─────────────────────────────────────┐
│ • Public routes: /, /products       │
│ • Auth routes: /orders, /checkout   │
│ • Admin routes: /admin/*            │
└─────────────────────────────────────┘

Layer 2: Authentication
┌─────────────────────────────────────┐
│ • Appwrite session management       │
│ • Token-based authentication        │
│ • Automatic session refresh         │
└─────────────────────────────────────┘

Layer 3: Authorization
┌─────────────────────────────────────┐
│ • User preferences check            │
│ • isAdmin: true validation          │
│ • Client-side route guards          │
└─────────────────────────────────────┘

Layer 4: Data Validation
┌─────────────────────────────────────┐
│ • Form validation                   │
│ • Type checking (TypeScript)        │
│ • Appwrite schema validation        │
└─────────────────────────────────────┘

Layer 5: API Security
┌─────────────────────────────────────┐
│ • API key in .env.local             │
│ • CORS configuration                │
│ • Appwrite permissions              │
└─────────────────────────────────────┘
```

## Deployment Architecture

```
┌──────────────────────────────────────────────────────────────┐
│                    DEPLOYMENT STACK                           │
└──────────────────────────────────────────────────────────────┘

┌─────────────────┐
│   FRONTEND      │
│   Next.js App   │
│                 │
│ Deploy to:      │
│ • Vercel        │
│ • Netlify       │
│ • Self-hosted   │
└────────┬────────┘
         │
         │ HTTPS
         │
┌────────▼────────┐
│   APPWRITE      │
│   Cloud/Self    │
│                 │
│ • Database      │
│ • Storage       │
│ • Auth          │
└─────────────────┘

Environment Variables:
• NEXT_PUBLIC_* (client-side)
• APPWRITE_API_KEY (server-side)
```

## Performance Optimizations

```
┌──────────────────────────────────────────────────────────────┐
│                    PERFORMANCE FEATURES                       │
└──────────────────────────────────────────────────────────────┘

1. Server Components
   • Products page fetches on server
   • Reduces client-side JavaScript
   • Faster initial page load

2. Code Splitting
   • Automatic route-based splitting
   • Lazy loading of components
   • Smaller bundle sizes

3. Image Optimization
   • Next.js Image component
   • Automatic format selection
   • Responsive images

4. State Persistence
   • Cart saved in localStorage
   • Survives page refreshes
   • No unnecessary API calls

5. Efficient Queries
   • Appwrite query filters
   • Pagination support
   • Indexed collections
```

---

**Architecture Status**: ✅ Production-Ready

**Scalability**: Horizontal scaling via Appwrite Cloud

**Maintainability**: Modular, typed, well-documented

**Security**: Multi-layer protection

**Performance**: Optimized for speed and efficiency
